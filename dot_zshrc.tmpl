# vi: ft=zsh


#--------------------------#
#  Update  Plugins         #
#--------------------------#


# eval fastfetch

# install zsh plugins
if [[ ! -e ~/.zsh-plugins/zsh-syntax-highlighting ]]; then
  git clone --depth=1 https://github.com/zsh-users/zsh-syntax-highlighting.git ~/.zsh-plugins/zsh-syntax-highlighting
fi
if [[ ! -e ~/.zsh-plugins/zsh-autosuggestions ]]; then
  git clone --depth=1 https://github.com/zsh-users/zsh-autosuggestions.git ~/.zsh-plugins/zsh-autosuggestions
fi
if [[ ! -e ~/powerlevel10k ]]; then
  git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ~/powerlevel10k
fi
if [[ ! -e ~/.zsh-plugins/zoxide ]]; then
  git clone --depth=1 https://github.com/ajeetdsouza/zoxide.git ~/.zsh-plugins/zoxide
fi
if [[ ! -e ~/.zsh-plugins/fzf ]]; then
  git clone --depth 1 https://github.com/junegunn/fzf.git ~/.zsh-plugins/fzf
  source ~/.zsh-plugins/fzf/install
fi
if [[ ! -e ~/.zsh-plugins/zsh-vi-mode ]]; then
  git clone --depth 1 https://github.com/jeffreytse/zsh-vi-mode.git ~/.zsh-plugins/zsh-vi-mode
fi


# install nnn plugins
if [[ ! -e ~/.config/nnn/plugins ]]; then
  sh -c "$(curl -Ls https://raw.githubusercontent.com/jarun/nnn/master/plugins/getplugs)"
fi

# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
  source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
fi

#zmodload zsh/zprof


# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=1000 
SAVEHIST=1000
setopt autocd extendedglob
unsetopt beep
# bindkey -v
# End of lines configured by zsh-newuser-install


#--------------------------#
#      Plugin config       #
#--------------------------#


# zsh-autocomplete plugin



# The following lines were added by compinstall
# zstyle :compinstall filename '/home/egrazil/.zshrc'

# autoload -Uz compinit
# compinit
# End of lines added by compinstall
#

# configure zsh-autosuggestions
ZSH_AUTOSUGGEST_MANUAL_REBIND=1


# zsh-vi-mode config
ZVM_VI_INSERT_ESCAPE_BINDKEY=hh


# nnn cd on quit 
nnn ()
{
    # Block nesting of nnn in subshells
    [ "${NNNLVL:-0}" -eq 0 ] || {
        echo "nnn is already running"
        return
    }

    # The behaviour is set to cd on quit (nnn checks if NNN_TMPFILE is set)
    # If NNN_TMPFILE is set to a custom path, it must be exported for nnn to
    # see. To cd on quit only on ^G, remove the "export" and make sure not to
    # use a custom path, i.e. set NNN_TMPFILE *exactly* as follows:
    #      NNN_TMPFILE="${XDG_CONFIG_HOME:-$HOME/.config}/nnn/.lastd"
    export NNN_TMPFILE="${XDG_CONFIG_HOME:-$HOME/.config}/nnn/.lastd"

    # Unmask ^Q (, ^V etc.) (if required, see `stty -a`) to Quit nnn
    # stty start undef
    # stty stop undef
    # stty lwrap undef
    # stty lnext undef

    # The command builtin allows one to alias nnn to n, if desired, without
    # making an infinitely recursive alias
    command nnn "$@"

    [ ! -f "$NNN_TMPFILE" ] || {
        . "$NNN_TMPFILE"
        rm -f -- "$NNN_TMPFILE" > /dev/null
    }
}


#--------------------------#
#        keybinds          #
#--------------------------#

# I think this sets up vi keybinds
bindkey -v

# ctrl right arrow
bindkey "^[[1;5C" forward-word
# ctrl left arrow
bindkey "^[[1;5D" backward-word

#--------------------------#
#        aliases           #
#--------------------------#

alias ls='ls --color=auto'
alias la='ls -a --color=auto'
alias grep='grep --color=auto'

alias sway='sway --unsupported-gpu'

eval $(thefuck --alias)

# Chezmoi aliases
alias cm='chezmoi'
alias cma='chezmoi apply -v'
alias cmra='chezmoi re-add -v'
alias cme='chezmoi edit'


# nnn aliases
alias nnn='nnn -deA -a'


#zprof
#source ~/powerlevel10k/powerlevel10k.zsh-theme

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
#[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

#--------------------------#
#          PATH            #
#--------------------------#

export PATH=$PATH:/home/egrazil/bin

# custom scripts folder
export PATH=$PATH:/home/egrazil/.local/bin

# add rust packages to path
export PATH=$PATH:/home/egrazil/.cargo/bin

# Path for egrazilhomeserver
# begin template for egrazil_home_server
{{- if eq .chezmoi.hostname "egrazil_home_server" }}

# setup homebrew on the path
eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"

{{- end }}
# end template for egrazil_home_server

#--------------------------#
#       ENV VARS           #
#--------------------------#

export EDITOR=nvim


# for exposway, a sway plugin I decided not to use
#export EXPOSWAYDIR="$HOME/.local/state/exposway/"
#export EXPOSWAYMON="$EXPOSWAYDIR/output"

# WINDOW TITLE

#autoload -Uz add-zsh-hookfunction xterm_title_precmd () {
#	print -Pn -- '\\e\]2;%n@%m %\~\\a'
#	\[\[ "$TERM" == 'screen'\* \]\] && print -Pn -- '\\e\_\\005{g}%n\\005{-}@\\005{m}%m\\005{-} \\005{B}%\~\\005{-}\\e\\\\'
#}
#function xterm_title_preexec () {

#print -Pn -- '\e]2;%n@%m %~ %# ' && print -n -- "${(q)1}\a"
#\[\[ "$TERM" == 'screen'\* \]\] && { print -Pn -- '\\e\_\\005{g}%n\\005{-}@\\005{m}%m\\005{-} \\005{B}%\~\\005{-} %# ' && print -n -- "${(q)1}\\e\\\\"; }
#}
# if [[ "$TERM" == (alacritty*|gnome*|konsole*|putty*|rxvt*|screen*|tmux*|xterm*) ]]; then

#add-zsh-hook -Uz precmd xterm\_title\_precmd
#add-zsh-hook -Uz preexec xterm\_title\_preexec
# fi

#########################
# configuration for nnn #
#########################

# set plugin keybinds
export NNN_PLUG='z:autojump;d:dragdrop;u:getplugs;o:fzplug;p:preview-tui'

# enable file icons in front of directory previews with .iconlookup
export NNN_ICONLOOKUP=1

# put hidden files on top
export LC_COLLATE="C"

# set image preview to use ueberzug
# export NNN_PREVIEWIMGPROG="ueberzug"
# export UEBERZUGPP_WAYLAND=1

# Set theme


# from https://github.com/catppuccin/catppuccin/discussions/1955 
# I also need to grab something else from there for the .iconlookup
#
#
# # The first option is a translation of the catppuccin colors to their closest match in a 256 color scheme (dependency free)
# BLK="E5" CHR="E5" DIR="99" EXE="97" REG="07" HARDLINK="E1" SYMLINK="E1" MISSING="08" ORPHAN="D3" FIFO="9F" SOCK="E5" UNKNOWN="D3"

# # Export Context Colors
# export NNN_COLORS="#9997E5D3;4231"

# This second option relies on you're terminal using the catppuccin theme and well use true catppuccin colors:
BLK="03" CHR="03" DIR="04" EXE="02" REG="07" HARDLINK="05" SYMLINK="05" MISSING="08" ORPHAN="01" FIFO="06" SOCK="03" UNKNOWN="01"

# Export Context Colors
export NNN_COLORS="#04020301;4231"

# Finally Export the set file colors ( Both options require this)
export NNN_FCOLORS="$BLK$CHR$DIR$EXE$REG$HARDLINK$SYMLINK$MISSING$ORPHAN$FIFO$SOCK$UNKNOWN"




#--------------------------#
#      Load Plugins        #
#--------------------------#

# set up fzf keybindings and fuzzy completion
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh

source ~/.zsh-plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
source ~/.zsh-plugins/zsh-autosuggestions/zsh-autosuggestions.zsh
source ~/powerlevel10k/powerlevel10k.zsh-theme
source ~/.zsh-plugins/zoxide/zoxide.plugin.zsh
source ~/.zsh-plugins/zsh-vi-mode/zsh-vi-mode.plugin.zsh
[[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh

